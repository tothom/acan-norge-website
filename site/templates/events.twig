{% extends 'default.twig' %}

{% block content %}
    <ul class="card-list">
        {% for event in events %}
            <li class="card">
                {{ include('@snippets/event-card.twig', {event: event})}}
            </li>
        {% endfor %}
    </ul>
{% endblock %}
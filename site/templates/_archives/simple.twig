{% set darkmode = false %}

{% extends 'default.twig' %}


{% block content %}
    <article>
        <h1 class="h1">{{ page.title.esc() }}</h1>
        <div class="text">
            {{ page.text.kt | raw }}
        </div>
    </article>
{% endblock %}
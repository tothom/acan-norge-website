{% extends 'events.twig' %}

{% block content %}
    <article class="event">
        <header class="event-header">
            <h1 class="event-title">{{ page.title.esc() }}</h1>
            <div class="autogrid">
                <div class="column">

                    <time class="h5" datetime="{{ page.date.toDate('c') }}">
                        {{ page.date.toDate('d/m/Y') }}
                    </time>
                    <br/>

                    {% if page.content().link() is not empty %}
                        <a class="h5" href="{{ page.content().link() }}">Link</a>
                        <br/>
                    {% endif %}

                    <p class="h6">{{ page.description }}</p>
                </div>
                <div class="column">
                    <div>
                        {% set coverImage = page.cover.toFile() %}

                        {% if coverImage %}
                            <img src="{{ coverImage.url() }}" class="img"
                                 style="--w:2; --h:1">
                        {% endif %}
                    </div>
                </div>
            </div>
        </header>

        <div class="event text">{{ page.text.kt | raw }}</div>



    </article>
{% endblock %}